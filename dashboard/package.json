{
  "name": "mvm-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bun run build && wrangler pages deploy .svelte-kit/cloudflare --project-name mvm-dashboard",
    "migrate:d1": "wrangler d1 execute mvm-prod --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^4.7.0",
    "@sveltejs/kit": "^2.5.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0"
  }
}
